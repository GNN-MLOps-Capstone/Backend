# ==============================================================================
# Python 패키지 의존성 목록 (requirements.txt)
# ==============================================================================
#
# 이 파일은 프로젝트에 필요한 Python 패키지들을 나열합니다.
#
# 설치 명령어:
#   pip install -r requirements.txt
#
# 버전 고정의 중요성:
#   패키지==버전 형식으로 버전을 고정하면
#   언제 어디서 설치해도 동일한 환경을 보장합니다.
#
# ==============================================================================


# =============================================================================
# 웹 프레임워크
# =============================================================================

# FastAPI - 고성능 비동기 웹 프레임워크
# 특징: 자동 API 문서 생성, 타입 힌트 기반 검증
# 공식 문서: https://fastapi.tiangolo.com/
fastapi==0.115.6

# Uvicorn - ASGI 서버 (FastAPI 실행용)
# [standard]: 추가 기능 포함 (websocket, 자동 재시작 등)
# 공식 문서: https://www.uvicorn.org/
uvicorn[standard]==0.34.0


# =============================================================================
# 데이터베이스
# =============================================================================

# SQLAlchemy - Python ORM (Object-Relational Mapping)
# SQL 대신 Python 코드로 DB 조작 가능
# 공식 문서: https://www.sqlalchemy.org/
sqlalchemy==2.0.36

# aiosqlite - 비동기 SQLite 드라이버
# SQLite를 async/await로 사용 가능하게 해줌
# 공식 문서: https://aiosqlite.omnilib.dev/
aiosqlite==0.20.0


# =============================================================================
# 데이터 검증
# =============================================================================

# Pydantic - 데이터 검증 라이브러리
# API 요청/응답의 데이터 형식을 검증
# 공식 문서: https://docs.pydantic.dev/
pydantic==2.10.4

# Pydantic Settings - 환경 변수 관리
# .env 파일에서 설정을 읽어옴
pydantic-settings==2.7.1


# =============================================================================
# 유틸리티
# =============================================================================

# python-dotenv - .env 파일 로드
# 환경 변수를 .env 파일에서 읽어옴
# 공식 문서: https://pypi.org/project/python-dotenv/
python-dotenv==1.0.1

# Pandas - 데이터 분석 라이브러리
# CSV 파일 읽기/쓰기에 사용
# 공식 문서: https://pandas.pydata.org/
pandas==2.2.3


# =============================================================================
# 추가 패키지 (필요시 주석 해제)
# =============================================================================

# PostgreSQL 비동기 드라이버 (프로덕션 DB 사용 시)
asyncpg==0.30.0

# MySQL 비동기 드라이버 (MySQL 사용 시)
# aiomysql==0.2.0

# 테스트 도구
# pytest==8.0.0
# pytest-asyncio==0.23.0

# httpx==0.27.0  # FastAPI 테스트용 HTTP 클라이언트

# 제미나이
google-genai==1.56.0
#구글 인증
google-auth>=2.45.0
# 구글 토큰 검증시 사용
requests==2.32.3
# access token발급
python-jose[cryptography]==3.3.0
# HTTP 클라이언트 (KIS API)
httpx==0.27.0

# WebSocket 클라이언트 (KIS 실시간)
websockets==12.0
